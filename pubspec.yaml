# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃ 📜 pubspec.yaml - Manifesto ritualístico do projeto Tutor                  ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

name: tutor
description: Sistema ritualistico para geracao e validacao de artefatos magicos
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  path: ^1.9.0
  yaml: ^3.1.2
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  genesis: genesis

# Sugestões
# - 🛡️ Adicionar `build_runner` se quiser gerar código automaticamente
# - 🔤 Incluir `dart_style` para formatação ritualística dos artefatos
# - 📦 Criar scripts de invocação em `tool/` para modularizar comandos
# - 🧩 Definir `assets/` se houver recursos mágicos externos
# - 🎨 Integrar com `melos` se quiser gerenciar múltiplos grimórios

# ✍️ byThyrrel  
# 💡 Manifesto formatado com estilo técnico, seguro e elegante  
# 🧪 Ideal para conjuradores de código com foco em automação limpa e confiável
