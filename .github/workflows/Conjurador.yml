name: Conjurador

on:
  workflow_dispatch:

jobs:
  invocar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1

      - name: Executar Conjurador Inteligente
        run: dart instrumentos_magicos/conjurador.dart
